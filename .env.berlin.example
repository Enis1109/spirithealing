# Preparation only. Never enable before the release checks in BERLIN-MEASUREMENT-SETUP.md.
VITE_BERLIN_MEASUREMENT_ENABLED=false
# The legacy consent-free campaign-reference switch has been removed.
VITE_META_PIXEL_ENABLED=false
VITE_META_POLICY_APPROVED=false
# Public ID created with user approval on 2026-09-17; not a secret. All activation gates remain false.
VITE_META_PIXEL_ID=971195529371016
BERLIN_MEASUREMENT_ENABLED=false
BERLIN_STRIPE_MODE=test
# JSON mapping using separate Stripe TEST Payment Link IDs (not checkout URLs).
# Required keys: own_full, intensive_full. Optional: own_first, intensive_first,
# own_followup, intensive_followup. Live IDs, duplicate IDs and unknown keys rejected.
# Use a dedicated test database and separate signing secret, never the live database.
BERLIN_STRIPE_TEST_LINKS=
# Set only in the hosting secret store. Never commit a real signing secret.
BERLIN_STRIPE_WEBHOOK_SECRET=
